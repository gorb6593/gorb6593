1	@RequestMapping("/emp/idcheck.do")
2	public ModelAndView idCheck(String id) {
3		ModelAndView mav = new ModelAndView();
4		boolean state = service.idCheck(id);
5		String result = "";
6		if(state) { 
7			result = "사용 불가능한 아이디";
8		}else {
9			result = "사용 가능한 아이디";
10		}
11		mav.setViewName("emp/list");
12		mav.addObject("result",result);
13		return mav;
14	}

1번 ( ) 안에 매핑주소 설정 / view의 요청 경로 아무거나 해도 됌

2번 ModelAndView 라고 idChechk클래스를 만들고 String 타입의 id를 받아오고 싶어서  idCheck(String id)이렇게 만들었음
	ModelAndView의 사용 이유 -> model에 저장하고 view를 할 수있어서

3번 ModelAndView  객체생성 

4번 id를 받아서 로그인이 됐는지 아닌지 하기 위해서 state에 그 값을 받아놓은 것

5번 result 값 초기화

6번 사용자가 입력한 id가 db에 이미 저장되어 있다는 의미
7번 db에 있으니까 가입이 안됌
8번 아니면
9번 db에 없으니까 가입이 됌
10번

11번 view의 이름 / ( )-> 뷰의 경로
12번 view로 보낼 값 / (   ,   )  -> ("변수 이름", "데이터 값")